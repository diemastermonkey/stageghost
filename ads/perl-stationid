< >
^C
<>monkey@dronesound:~ $ <
>monkey@dronesound:~ $ <cat > RandomStationID
#!/usr/>bin</perl
# Select and spit-out a random stationid
$ads = `ls -1 station_ids/*.txt`;
@all = split (/\n/, $ads);
$r = int(rand(scalar(@all)));
$ad = @all[$r];
$e = `cat $ad`;
print $e;
^C
<>monkey@dronesound:~ $ <./RandomStationID>

